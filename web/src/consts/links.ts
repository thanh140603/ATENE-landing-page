/**
 * Owner tasks #1 & #2:
 * - Paste your Application form URL into `APPLICATION_FORM_URL` (申込フォームURL)
 * - Paste your Live booking URL into `LIVE_BOOKING_URL` (予約確定URL)
 */
export const APPLICATION_FORM_URL = '申込フォームURL'
export const LIVE_BOOKING_URL = '予約確定URL'

export const CONTACT = {
  lineId: 'LINE_ID',
  email: 'contact@example.com',
  name: 'Contact Name',
} as const

